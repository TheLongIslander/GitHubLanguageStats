#ifndef PURE_OAUTH_SERVER_HPP
#define PURE_OAUTH_SERVER_HPP

#include <string>

// Starts a lightweight HTTP listener and returns the OAuth code
std::string waitForOAuthCode();

#endif // PURE_OAUTH_SERVER_HPP

#ifndef PRINT_SORTED_HPP
#define PRINT_SORTED_HPP

#include <string>
#include <unordered_map>

void printSortedLanguageStats(const std::unordered_map<std::string, int>& lang_totals);

#endif // PRINT_SORTED_HPP
